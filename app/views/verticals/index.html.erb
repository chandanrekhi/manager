<div class="block">
  <div class="secondary-navigation">
    <ul class="wat-cf">
      <li class="first active"><%= link_to "#{t("web-app-theme.list", :default => "List")}", verticals_path  %></li>
      <li><%= link_to "#{t("web-app-theme.new", :default => "New")}", new_vertical_path %></li>
    </ul>
  </div>          
  <div class="content">          
    <h2 class="title"><%= t("web-app-theme.all", :default => "All")  %> Verticals</h2>
    <div class="inner">
      <table class="table">
        <tr>             
          <th class="first">ID</th>
                    <th>
            <%= t("activerecord.attributes.vertical.name", :default => t("activerecord.labels.name", :default => "Name")) %>
          </th>
                    <th><%= t("web-app-theme.created_at", :default => "Created at")  %></th>
          <th class="last">&nbsp;</th>
        </tr>
        <% @verticals.each do |vertical| -%>
        <tr class="<%= cycle("odd", "even") %>">
          <td>
            <%= vertical.id %>
          </td>
                    <td>
            <%= link_to vertical.name, vertical_path(vertical) %>
          </td>
                    <td>
            <%= vertical.created_at %>
          </td>
          <td class="last">
            <%= link_to "#{t("web-app-theme.show", :default => "Show")}", vertical_path(vertical) %> |
            <%= link_to "#{t("web-app-theme.edit", :default => "Edit")}", edit_vertical_path(vertical) %> |
            <%= link_to "#{t("web-app-theme.delete", :default => "Delete")}", vertical_path(vertical), :method => :delete, :confirm => "#{t("web-app-theme.confirm", :default => "Are you sure?")}" %>
          </td>
        </tr>          
        <% end -%>
      </table>
      <div class="actions-bar wat-cf">
        <div class="actions">
        </div>
        
      </div>
    </div>
  </div>
</div>

<% content_for :sidebar, render(:partial => 'sidebar') -%>
